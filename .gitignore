__pycache__/
*.pyc
.venv/
data/_cache/
_build/
node_modules/

# Credential files for live checks (--env-file). Keep them outside the repo if
# you can; this is a backstop, not permission.
*.env
.env
secrets/
