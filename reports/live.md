# Live checks

What happens when a cookbook is actually built and run on [Project Pythia's BinderHub](https://binder.projectpythia.org) — the environment its launch button sends learners to. Generated by `scripts/report_live.py`; do not edit.

Read [the method and its limitations](../docs/live-assessment.md) before quoting any number here. In particular, a cached image makes build time meaningless, and a single failed run of a network-dependent notebook is a prompt to re-run rather than a finding.

## HRRR-AWS-cookbook

Run 2026-07-21 18:16:28 UTC against [https://binder.projectpythia.org](https://binder.projectpythia.org), ref `main`.

:::{warning} Cached image
BinderHub reused an existing image, so the 6.4s is a pod launch and image pull. It does **not** test whether `environment.yml` still solves.
:::

| Measure | Value |
|---|---|
| Time to a ready session | 6.4s (cached image) |
| Build succeeded | yes |
| Notebook execution | 78.53s |
| Execution succeeded | yes |
| Peak memory (rss) | 0.63 GB |
| Pod memory limit | 8.59 GB |
| Peak as share of limit | 7.4% |
| Errors raised | 0 |

| Notebook | Execute + render |
|---|---|
| `notebooks/example-workflows/plot-2mt.ipynb` | 22.0s |

Executed 1 notebook(s) from the project toc: `notebooks/example-workflows/plot-2mt.ipynb`. Notebooks not listed in `myst.yml` are never executed by a build.

